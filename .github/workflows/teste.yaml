on:
  workflow_dispatch:
    inputs:
        name:
          description: Username
          required: false

jobs:
  teste:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        
      - name: Teste
        uses: ./composite-action-2
